/* eslint-env mocha */
var assert = require('assert')

describe('GetFromServer', function () {
  describe('#indexOf()', function () {
    it('should return 202 code', function () {
      assert.strictEqual([1, 2, 3].indexOf(4), -1)
    })
  })
})
